name: Keep Repl Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping repl
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            https://80570bc7-dd85-4fce-9593-0a8ae7c7c94d-00-pen3vhnpm8mz.spock.replit.dev/ \
            || true
